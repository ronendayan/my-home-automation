System:
  view: yes
  name: "System"
  icon: mdi:settings
  entities:
    - group.servers
    - group.processes
    - group.storage
    - group.speedtest
    - group.transmission
    - group.certificates
    - group.automations
    - group.motion_switches
    - group.temperatures
