System:
  view: yes
  name: "System"
  entities:
    - group.servers
    - group.processes
    - group.storage
    - group.speedtest
    - group.certificates
    - group.automations
    - camera.kids_room
    - camera.living_room
    - group.motion
    - group.motion_switches
